---
deployment:
  tasks:
    - export DEPLOYPATH=/home/varolver/public_html/deneme/menu/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/rsync -a --delete --exclude='.git' --exclude='.cpanel.yml' --exclude='config.php' --exclude='admin/config.php' ./ $DEPLOYPATH
